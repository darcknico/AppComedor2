<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aldebaran.appcomedor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_comedor"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_comedor"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".TicketsActivity" />
        <activity android:name=".comprarMenu" />
        <activity android:name=".verTicket" />
        <activity android:name=".crearTransaccion" />
        <activity android:name=".VerTransacciones" />
        <activity android:name=".HomeActivity" />
    </application>

</manifest>